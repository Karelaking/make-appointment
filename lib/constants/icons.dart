import 'package:flutter/material.dart';

class AppIconConstant {
  // Arrow Back
  Icon arrowBack(context) => Icon(
        Icons.arrow_back_ios,
        color: Theme.of(context).primaryIconTheme.color,
      );
  // Arrow Forward
  Icon arrowForward(context) => Icon(
        Icons.arrow_forward_ios,
        color: Theme.of(context).primaryIconTheme.color,
      );
  // Switch Theme
  Icon switchTheme(context) => Icon(
        Icons.toggle_off_outlined,
        color: Theme.of(context).primaryIconTheme.color,
        size: 26,
      );
  // Languages
  Icon language(context) => Icon(
        Icons.language_rounded,
        color: Theme.of(context).primaryIconTheme.color,
        size: 26,
      );
  // About
  Icon about(context) => Icon(Icons.info_rounded,
      color: Theme.of(context).primaryIconTheme.color, size: 26);
  // Clear
  Icon clear(context) => Icon(Icons.cleaning_services_rounded,
      color: Theme.of(context).primaryIconTheme.color, size: 26);
  // FeedBack
  Icon feedBack(context) => Icon(Icons.thumb_up_off_alt_sharp,
      color: Theme.of(context).primaryIconTheme.color, size: 26);
  // Thumbs Up
  Icon thumbsUp(context) => Icon(Icons.thumb_up_off_alt_sharp,
      color: Theme.of(context).primaryIconTheme.color, size: 100);
  // Create Account
  Icon createAccount(context) => Icon(Icons.thumb_up_off_alt_sharp,
      color: Theme.of(context).primaryIconTheme.color, size: 26);
  // Login
  Icon login(context) => Icon(Icons.login_rounded,
      color: Theme.of(context).primaryIconTheme.color, size: 26);
  // Logout
  Icon logOut(context) => Icon(Icons.logout_rounded,
      color: Theme.of(context).primaryIconTheme.color, size: 26);
  // Delete Account
  Icon deleteAccount(context) => Icon(Icons.delete_rounded,
      color: Theme.of(context).primaryIconTheme.color, size: 26);
  // Terms And Condition
  Icon termsAndConditiion(context) => Icon(Icons.copy_outlined,
      color: Theme.of(context).primaryIconTheme.color, size: 26);
  // Data Privacy
  Icon dataPrivacy(context) => Icon(Icons.file_copy,
      color: Theme.of(context).primaryIconTheme.color, size: 26);
  // Question And Answer
  Icon location(context) => Icon(Icons.location_on,
      color: Theme.of(context).primaryIconTheme.color, size: 26);
  // Location
  Icon dateTime(context) => Icon(Icons.alarm_add_rounded,
      color: Theme.of(context).primaryIconTheme.color, size: 26);
  // Date and time
  Icon phone(context) => Icon(Icons.phone,
      color: Theme.of(context).primaryIconTheme.color, size: 26);
  // Phone
  Icon email(context) => Icon(Icons.email,
      color: Theme.of(context).primaryIconTheme.color, size: 26);
  // Question and answer
  Icon qAndA(context) => Icon(Icons.question_mark_rounded,
      color: Theme.of(context).primaryIconTheme.color, size: 26);
  // Reason
  Icon reason(context) => Icon(Icons.info_rounded,
      color: Theme.of(context).primaryIconTheme.color, size: 26);
  // Add
  Icon add(context) => Icon(Icons.add,
      color: Theme.of(context).primaryIconTheme.color, size: 26);
  // Share
  Icon share(context) => Icon(Icons.share_rounded,
      color: Theme.of(context).primaryIconTheme.color, size: 26);
  // Menu
  Icon menu(context) => Icon(Icons.more_vert_rounded,
      color: Theme.of(context).primaryIconTheme.color, size: 26);
  // Person
  Icon person(context) => Icon(Icons.person,
      color: Theme.of(context).primaryIconTheme.color, size: 26);
  // Star
  Icon star(context) => Icon(Icons.star_rounded,
      color: Theme.of(context).primaryIconTheme.color, size: 26);
  // Delete
  Icon delete(context) => Icon(Icons.delete_rounded,
      color: Theme.of(context).primaryIconTheme.color, size: 26);
  // Calendar
  Icon calander(context) => Icon(Icons.calendar_month_rounded,
      color: Theme.of(context).primaryIconTheme.color, size: 26);
  // Notifications 
  Icon notification(context) => Icon(Icons.notifications,
      color: Theme.of(context).primaryIconTheme.color, size: 26);
  // Settings
  Icon settings(context) => Icon(Icons.settings_rounded,
      color: Theme.of(context).primaryIconTheme.color, size: 26);
  // Calendar  Welcome
  Icon calanderWelcome(context) => Icon(Icons.calendar_month_rounded,
      color: Theme.of(context).primaryIconTheme.color, size: 100);
  // Cloude
  Icon cloude(context) => Icon(Icons.cloud_circle_rounded,
      color: Theme.of(context).primaryIconTheme.color, size: 100);
  // Start
  Icon start(context) => Icon(Icons.start_rounded,
      color: Theme.of(context).primaryIconTheme.color, size: 100);
}
